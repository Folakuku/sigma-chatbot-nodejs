<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Chatbot</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <header>
        <nav>
            <h1>AI Chatbot</h1>
            <% if (user) { %>
                <h1>Hi <%= user.username %>
                </h1>
                <a href="/chat">Chat</a>
                <a href="/chat/create-character">Create Character</a>
                <a href="/logout">Logout</a>
                <% } else { %>
                    <a href="/login">Login</a>
                    <a href="/signup">Signup</a>
                    <% } %>
        </nav>
        <div>
            <% if (error && error.length) { %>
                <div class="error">
                    <%= error %>
                </div>
                <% } %>
                    <% if (success && success.length) { %>
                        <div class="message">
                            <%= success%>
                        </div>
                        <% } %>

        </div>
    </header>
    <main>